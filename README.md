# Cool.ts

Framework for building web applications.

## How to run

1. Clone this repository
2. Request me to send source code of core folder(framework files)
3. Run `npm install`
4. Run `npm run dev`
5. Open `http://localhost:5173` to watch demo